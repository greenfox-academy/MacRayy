# Write a program that asks for an integer that is a distance in kilometers,
# then it converts that value to miles and prints it

user = input("enter km: ")

mile = int(user) * 0.62

print(str(user) + " km is " + str(mile) + " mile")
